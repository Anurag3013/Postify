<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Postify - Edit Post</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <div class="container navbar">
            <a href="/posts" class="brand">Postify</a>
            <a href="/posts" class="btn btn-secondary">Cancel</a>
        </div>
    </header>

    <main class="container">
        <div class="card" style="max-width: 600px; margin: 0 auto;">
            <h1 class="mb-4">Edit Post</h1>
            
            <div class="mb-4 text-muted">
                Post ID: <%=post.id %> <br>
                Username: @<%=post.username %>
            </div>
            
            <form method="post" action="/posts/<%=post.id%>?_method=PATCH">
                <div class="form-group">
                    <label for="content">Content</label>
                    <!-- Make sure to trim whitespace for textarea content -->
                    <textarea id="content" name="content" required><%= post.content.trim() %></textarea>
                </div>
                
                <div class="action-bar" style="justify-content: flex-end; border: none; padding-top: 0;">
                    <button class="btn btn-primary">Save Changes</button>
                </div>
            </form>
        </div>
    </main>
</body>
</html>